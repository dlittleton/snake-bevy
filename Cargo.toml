[package]
name = "snake-bevy"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.18.1"
bevy_prng = { version = "0.14.2", features = ["wyrand"] }
bevy_rand = { version = "0.14.2" }
rand_core = "0.10.0"

[profile.release]
opt-level = 'z'
lto = true
codegen-units = 1
